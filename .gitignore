
# Delivery artifacts, not part of the website. Netlify publishes the whole
# repo, so anything sitting here is served to the internet and metered - the
# two zips alone were 11.7MB of the 25MB site.
*.zip
store/
promo/

# Built into the app by the workflow, not kept in the repo.
android/app/src/main/assets/
android/.gradle/
android/build/
android/app/build/
promo-mp4/
tools/cards/
tools/node_modules/

# Rendered video and the ffmpeg binary the build script pulls in. Delivery
# artifacts and tooling, not part of the site.
promo-mp4/
tools/cards/
tools/node_modules/
