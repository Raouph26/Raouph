<resources>
    <!-- Black behind the game so the first frame matches the studio card the
         page opens with, instead of flashing white on launch. -->
    <style name="Theme.Wordspyre" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">#0B0B0D</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#00000000</item>
    </style>
</resources>
