# Filament

A calm line-drawing puzzle game for phones. TypeScript + Canvas, no engine, no
runtime dependencies, ~25 kB total.

> "Filament" is a working title. The genre is open, but pick a name and art
> direction you own before publishing.

## The rules

A board is a grid of pieces. Some cells hold a coloured **shape** (triangle,
square, hexagon); some hold a neutral **hub** (the circle with arcs).

For each colour you draw **one continuous line**:

1. It starts at one **terminal** — a piece wearing a halo — and ends at the
   other terminal of the same colour.
2. It must pass through **every piece of its colour, exactly once**.
3. It moves between **touching cells, diagonals included**.
4. It may only travel over its own colour's pieces, or over hubs.

Two more rules bind the board together:

5. **A hub must be crossed exactly as many times as it shows arcs.** Any colour
   may cross a hub, and one line may cross it more than once, entering and
   leaving by different routes.
6. **Lines never overlap and never cross**, including two diagonals cutting
   through the same square. Hubs are the only place lines may meet.

No timer, no score, no way to lose.

## Playing

Press a terminal and drag. Drag backwards to undo a step. Press any piece
already on a line to grab it there and cut everything after it. Press a terminal
again to start that line over.

Solving a stage carries you to the next one by itself, with the finished board
sliding away as the next arrives. Touching a solved board cancels that, so
staying to admire or re-draw it is always possible.

A piece drawn **hollow** has no line through it yet; a **solid** piece is done.
That is the main way to read what is left without tracing every line by eye.

- **Classic** — 20 chapters of 32 stages. The first twelve stages of chapter 1
  are authored, not generated: each introduces exactly one idea, because the
  generator has no concept of teaching. Chapters 1–5 are three columns wide and
  use two colours; the third colour arrives at chapter 6, and boards grow from
  there. A chapter opens once 20 of the previous chapter's 32 are solved, so one
  hard puzzle never walls off the rest.
- **Daily** — 32 stages, reseeded from the calendar date. Capped at four columns:
  a wider board with three colours stops being a puzzle and becomes a search.

## Running it

```bash
npm install
npm run dev          # play at http://localhost:5173
npm test             # rules, plus a sample of the catalogue
npm run validate     # generates and verifies all 672 levels
npm run test:browser # drives real Chromium, solving levels with simulated drags
npm run build:artifact
```

## How it is put together

```
src/core/       the game, independent of any rendering
  types.ts      board and cell model
  level.ts      ASCII level format, parsing, validation
  rules.ts      every legality rule — the single source of truth
  game.ts       interactive state: press, drag, retract, truncate
  solver.ts     exhaustive backtracking search
  generator.ts  seeded generation, verified by the solver
  chapters.ts   the difficulty ramp and the level catalogue
src/render/     canvas drawing, palette, animation state
src/audio/      procedural synthesis
src/progress.ts solved state and unlock rules
```

Four decisions worth knowing:

**Every rule lives in `canExtend`.** Legality is enforced as you draw, not
checked at the end, so an illegal board is unreachable through play. The solver
and the player move through the same function — if the game lets you draw it,
the solver considers it, and vice versa.

**Puzzles are generated by drawing a solution first.** The generator
random-walks a line per colour, then reads the board off that drawing: cells
touched once become pieces, cells touched more than once become hubs with that
many arcs. A solution exists by construction, and the solver then discards any
board with more than one, so every level has exactly one answer.

**Generation budgets are counted in work, never in time.** All 672 levels are
built on the player's device from the level id. A wall-clock budget would let a
slow phone give up earlier than a fast one and produce a *different* puzzle for
the same id, so attempts and search nodes are capped instead — every device
agrees on every board.

**Difficulty comes from area and line count, not path length.** Benchmarking
showed long lines relative to the board are what make generation both slow and
ambiguous — a long line has too many equivalent routings. Bigger boards with
more, shorter lines stay fast to generate and are the more interesting kind of
hard.

## Audio

Synthesised at runtime; there are no audio files. Notes come from a major
pentatonic scale, which has no minor seconds and no tritone, so any combination
of them is consonant — lines drawn in any order, at any speed, cannot produce a
sour interval. Illegal moves are deliberately silent; a buzzer is the fastest
way to make a calm game stressful.

There is no background bed, on purpose. An earlier build held an ambient drone
through the reverb, and a *sustained* tone convolved with a noise impulse
response smears into audible hiss — the tail never decays, so the noise floor is
continuously re-excited. Only discrete notes reach the reverb now, and the
silence between them is left alone.

## Themes

Ten palettes, each written as a few grounds plus three accents; every derived
value (hollow fills, halos, line colours, hub marks) is computed from those, so
a theme cannot drift out of step with itself. The same definition drives the
canvas and the interface, so the board and the chrome around it always match.

Accents are always spread widely around the wheel rather than being three
neighbours, so the three families stay separable for colour-blind players in
every theme, not only the default.

**Every chapter wears its own theme**, cycling through the set, so the game keeps
changing as it is played. Clearing a chapter unlocks the right to *pin* a theme
and keep it everywhere — seeing a look and owning it are separate rewards.

## Status

Playable end to end: menu, chapter and stage select, classic and daily,
progress, audio, animation.

Not yet built: the Capacitor wrapper for iOS/Android store builds, a tutorial
that teaches hubs explicitly, and settings beyond the sound toggle.
