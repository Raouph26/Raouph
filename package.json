{
  "name": "lyne-like",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:browser": "tsx scripts/browser-check.ts",
    "validate": "tsx scripts/validate-chapters.ts",
    "difficulty": "tsx scripts/measure-difficulty.ts",
    "build:artifact": "vite build && tsx scripts/build-artifact.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
